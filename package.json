{
  "name": "projectcomplete",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "SERVER_TEST_REPORTER=xunit XUNIT_FILE=$CIRCLE_TEST_REPORTS/reports/unit.xml meteor test --once --driver-package dispatch:mocha"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "meteor-node-stubs": "~0.2.4"
  },
  "devDependencies": {
    "phantomjs-prebuilt": "^2.1.14"
  },
  "version": "0.0.1"
}
